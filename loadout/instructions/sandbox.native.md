## Sandbox

This machine's `unsandboxed` loadout profile launches agent CLIs without nono.
Each agent's own sandbox and approval settings still apply. Diagnose failures
using the active session's permissions and the tool's error output.
