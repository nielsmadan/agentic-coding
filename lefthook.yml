pre-commit:
  commands:
    sync-global:
      run: loadout check --global
    publish-manifest:
      run: python3 publish/sync.py --check-manifest --check-sources
    sandbox-writable:
      glob: "nono/*.json"
      run: ./bin/nono-audit --check
