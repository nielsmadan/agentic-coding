[[source]]
name = "ac"
path = "loadout"

# Destinations and renderers come from loadout's agent preset, which carries each
# harness's config-directory variable — so no machine path is named here.
# `permissions` and `mcp` render without being asked for; `instructions` needs an
# order and `settings` names an input, so both are stated.

# Everything an agent does not override. Codex and Pi take it as-is; Claude
# names its own instructions and keeps the rest.
[all]
instructions = [
    "intro-shared", "web-fetching.shared", "video-content", "browser-automation",
    "secrets", "sandbox", "agent-dispatch", "git-policy", "git-commit-policy", "working-style",
]

[claude]
settings = ["claude", "claude-afk"]
instructions = [
    "intro-claude", "web-fetching", "video-content", "browser-automation",
    "secrets", "sandbox", "agent-dispatch", "git-policy", "git-commit-policy", "working-style",
]

[codex]

# mcp/sync.py co-owns this document's `mcp` key and writes the same destination;
# `preserve` reads that key back out of the destination before overwriting.
[opencode]
settings = "opencode"
preserve = ["mcp"]

[pi]
